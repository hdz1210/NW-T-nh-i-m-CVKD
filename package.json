{
  "name": "diem-cvkd-gas",
  "version": "1.0.0",
  "description": "Google Apps Script CI/CD với Clasp và GitHub Actions",
  "scripts": {
    "test": "node --test tests/*.test.cjs",
    "version": "node scripts/inject-version.mjs",
    "push": "node scripts/inject-version.mjs && clasp push",
    "pull": "clasp pull",
    "deploy": "node scripts/inject-version.mjs && clasp push --force"
  },
  "devDependencies": {
    "@google/clasp": "^2.4.2"
  }
}
