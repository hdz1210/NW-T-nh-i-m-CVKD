{
  "name": "diem-cvkd-gas",
  "version": "1.0.0",
  "description": "Google Apps Script CI/CD với Clasp và GitHub Actions",
  "scripts": {
    "push": "clasp push",
    "pull": "clasp pull",
    "deploy": "clasp push --force"
  },
  "devDependencies": {
    "@google/clasp": "^2.4.2"
  }
}
